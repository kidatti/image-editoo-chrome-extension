# 画像編集アプリ

画像を読み込んで、図形や文字を追加できます。

## 機能

### 基本機能
- 画像ファイルの読み込み（初期画面のファイル読み込みボタンまたはドラッグ＆ドロップ）
- **クリップボードからの画像貼り付け（初期画面の貼り付けボタンまたはCtrl+V / Cmd+V）**
- キャンバス上での図形描画
- **キャンバス上での直接テキスト入力**
- 編集した画像の保存
- **画像全体のクリア（削除ボタン）**

### 描画ツール
- **四角形**: ドラッグで四角形を描画
- **角丸四角形**: より丸い角の四角形（半径20px）
- **円**: ドラッグで楕円を描画（四角形と同様に直感的なサイズ調整）
- **矢印**: 始点と終点を指定して矢印を描画
- **テキスト**: クリックした位置にテキストを直接入力
- **モザイク**: 選択範囲にモザイク効果を適用してプライバシー保護

### 編集機能
- **直接選択**: 図形を直接クリックして選択（青い破線でハイライト表示）
- **移動**: 選択された図形をドラッグで移動
- **リサイズ**: 選択された図形を8つのハンドルでサイズ変更
- **削除**: Delete/Backspaceキーで選択された図形を削除
- 選択されたオブジェクトの色変更
- 選択されたオブジェクトの線の太さ変更（図形）またはフォントサイズ変更（テキスト）
- リアルタイムでの変更反映
- **クリップボードへのコピー**: 編集した画像をクリップボードにコピー（他アプリで貼り付け可能）

### UI機能
- **左サイドバー**: 縦配置のツールバー
- **色選択**: プリセットカラーパレット + カスタムカラーピッカー
- **線の太さ**: 2, 4, 6, 8, 10px（視覚的プレビューのみ表示）
- **フォントサイズ**: 12, 16, 20, 24, 32px（テキスト選択時）
- **初期画面**: ファイル読み込みと貼り付けボタンを中央に配置

## ファイル構成

```
/Users/kidatti/wk/skitch/
├── index.html          # メインのHTMLファイル
├── style.css           # スタイルシート
├── script.js           # JavaScript機能実装
└── AGENTS.md          # このファイル（プロジェクト説明）
```

## 使い方

1. **画像の読み込み**
   - 初期画面中央の「Load Image」アイコンをクリックしてファイルを選択
   - または画像ファイルをキャンバス領域にドラッグ＆ドロップ
   - **初期画面の「Paste from Clipboard」アイコンをクリックまたはCtrl+V（Cmd+V）でクリップボードから画像を貼り付け**

2. **図形の描画**
   - 左サイドバーから描画したい図形を選択（四角形、角丸四角形、円、矢印）
   - キャンバス上でドラッグして図形を描画
   - **角丸四角形はより丸い角（半径20px）で描画**

   **モザイク効果の適用**
   - 左サイドバーの「モザイク」ツールを選択
   - キャンバス上でドラッグして範囲を指定
   - **選択した範囲にモザイク効果が適用され、プライバシー保護が可能**

3. **テキストの追加**
   - 左サイドバーの「テキスト」ツールを選択
   - キャンバス上の任意の位置をクリック
   - **その場に入力ボックスが表示され、直接テキストを入力**
   - Enterキーで確定、Escapeキーでキャンセル

4. **オブジェクトの選択と編集**
   - 任意のツールで既存の図形を直接クリックして選択
   - 選択されたオブジェクトは青い破線で囲まれ、8つのリサイズハンドルが表示
   - **ドラッグで移動、ハンドルでサイズ変更**
   - Delete/Backspaceキーで削除

5. **色とサイズの調整**
   - 色: カラーボタンをクリックしてプリセットまたはカスタムカラーを選択
   - 線の太さ/フォントサイズ: 太さボタンをクリック
     - **図形選択時: 線の太さ（2, 4, 6, 8, 10px）**
     - **テキスト選択時: フォントサイズ（12, 16, 20, 24, 32px）**

6. **画像の管理**
   - 削除ボタン: 画像全体をクリア（確認ダイアログあり）
   - **コピーボタン**: 編集した画像をクリップボードにコピー（他のアプリケーションで Ctrl+V / Cmd+V で貼り付け可能）
   - 保存ボタン: 編集した画像をPNGファイルとしてダウンロード

## 技術仕様

### 使用技術
- HTML5 Canvas API
- Vanilla JavaScript（ES6+）
- CSS3
- File API（画像読み込み）
- Clipboard API（クリップボード対応）

### ブラウザ対応
- 現代的なブラウザ（Chrome, Firefox, Safari, Edge）
- HTML5 Canvasをサポートするブラウザ
- **クリップボード機能（貼り付け・コピー）はHTTPS環境またはlocalhostでのみ動作**

### 機能詳細

#### ImageEditorクラス
- `setupCanvas()`: キャンバスの初期設定
- `loadImage()`: 画像ファイルの読み込みと表示
- `loadImageFromUrl()`: URLから画像を読み込み
- `handlePaste()`: クリップボードからの貼り付け処理
- `pasteFromClipboard()`: クリップボードAPI経由での画像取得
- `startDrawing()`, `draw()`, `stopDrawing()`: 図形描画の制御
- `redraw()`: キャンバス全体の再描画と選択ハイライト
- `drawEllipse()`: 楕円の描画ロジック
- `drawRoundedRect()`: 角丸四角形の描画ロジック（半径20px）
- `drawArrow()`: 矢印の描画ロジック
- `createInlineTextInput()`: キャンバス上での直接テキスト入力
- `applyMosaic()`: 選択範囲へのモザイク効果適用
- `selectShapeAt()`: 指定座標の図形を選択
- `moveShape()`, `resizeShape()`: 図形の移動とリサイズ
- `isPointInShape()`: 点が図形内にあるかの判定
- `updateUIForSelectedShape()`: 選択された図形の情報をUIに反映
- `clearCanvas()`: 画像全体のクリア
- `copyToClipboard()`: 編集結果をクリップボードにコピー
- `downloadImage()`: 編集結果の保存

#### UI機能
- **ポップアップベースの設定**: カラーピッカーと線の太さ/フォントサイズ選択
- **動的UI更新**: 選択されたオブジェクトに応じてUI要素を自動更新
- **フォントサイズ切り替え**: テキストツール選択時に太さボタンがフォントサイズ選択に切り替わる
- **初期画面**: アイコンベースのファイル操作インターフェース

#### イベント処理
- マウスイベント（描画、移動、リサイズ制御）
- ファイルドロップイベント（画像読み込み）
- キーボードイベント（Delete/Backspace削除、ESCキャンセル）
- **クリップボードイベント（Ctrl+V / Cmd+V での画像貼り付け）**

## 開発者向け情報

### カスタマイズポイント
- 新しい描画ツールは`drawPreview()`と`redraw()`メソッドに追加
- UIの改善は`style.css`で調整
- 新機能は`ImageEditor`クラスに追加

### 今後の拡張案
- 取り消し/やり直し機能
- レイヤー機能
- より多くの図形ツール
- 画像フィルター
- クラウド保存機能

## 実行方法

1. ローカルサーバーを起動（推奨）:
   ```bash
   # Python 3の場合
   python -m http.server 8000
   
   # Python 2の場合
   python -m SimpleHTTPServer 8000
   
   # Node.jsの場合
   npx serve .
   ```

2. ブラウザで `http://localhost:8000` にアクセス

または、`index.html`をブラウザで直接開く（一部機能が制限される場合があります）。
